import 'package:flutter/cupertino.dart';

class CollectionsTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("CollectionsTab"),
    );
  }
}